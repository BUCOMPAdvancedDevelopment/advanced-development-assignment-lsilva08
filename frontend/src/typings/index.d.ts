export type Product = {
    id: number;
    name: string;
    price: number;
    description: string;
}

export type User = {
    id: number;
    name: string;
    email: string;
    profile: string;
}